<h1>Listing debits</h1>

<table>
  <tr>
    <th>Friend</th>
    <th>Amount</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @debits.each do |debit| %>
  <tr>
    <td><%= debit.friend %></td>
    <td><%= debit.amount %></td>
    <td><%= debit.status %></td>
    <td><%= link_to 'Show', debit %></td>
    <td><%= link_to 'Edit', edit_debit_path(debit) %></td>
    <td><%= link_to 'Destroy', debit, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Debit', new_debit_path %>
